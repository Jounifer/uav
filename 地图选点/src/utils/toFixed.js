export default (name = '') => {

    if (String(n).indexOf('.') > 0 && String(n).substr(String(n).indexOf('.')+1).length > len) {
        return n.toFixed(len)
    } else {
        return n;
    }
  
  }
  